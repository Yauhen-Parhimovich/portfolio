import Layout from '../../HOC/Layout/Layout';

import './Skills.scss';

const Skills = () => {

  return (
    <section className="skills">
      <h1>Skills</h1>
    </section>

  );
};

export default Layout(Skills);
