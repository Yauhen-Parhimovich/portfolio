import Layout from '../../HOC/Layout/Layout';

import './About.scss';

const About = () => {

  return (
    <section className="about">
      <h1>About</h1>
    </section>

  );
};

export default Layout(About);
